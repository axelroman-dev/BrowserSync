# Server
server/node_modules/
server/dist/
server/.env
server/backups/

# Extension (no build step, but keep any packaged build artifacts out,
# wherever they land - e.g. the zip you build for the Chrome Web Store)
*.zip

# OS/editor cruft
.DS_Store
*.swp
